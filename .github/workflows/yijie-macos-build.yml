name: 颉AI Build (Unsigned)

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'desktop/**'
      - 'server/**'
      - 'shared/**'
      - 'core/**'
      - 'lib/**'
      - 'hub/**'
      - 'plugins/**'
      - 'skills2set/**'
      - 'packages/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/yijie-macos-build.yml'

jobs:
  macos-build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: arm64
            label: Apple-Silicon
          - arch: x64
            label: Intel

    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24.15.0"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build workspace packages
        run: npm run build:packages

      - name: Build client bundles
        run: npm run build:client

      - name: Build server bundle
        shell: bash
        run: node scripts/build-server.mjs darwin ${{ matrix.arch }}

      - name: Build Computer Use helper
        shell: bash
        run: |
          HANA_COMPUTER_USE_HELPER_ARCH=${{ matrix.arch }} node scripts/build-computer-use-helper.mjs ${{ matrix.arch }}

      - name: Package macOS (unsigned DMG)
        shell: bash
        run: |
          ulimit -n 65536
          npx electron-builder --mac dmg zip --${{ matrix.arch }} --publish never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"

      - name: Upload artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: 颉AI-macOS-${{ matrix.label }}
          retention-days: 30
          path: |
            dist/*.dmg
            dist/*.zip
          if-no-files-found: error

      - name: Debug - list dist
        if: failure()
        run: |
          echo "=== dist/ ==="
          ls -la dist/ || echo "dist/ not found"
          echo "=== dist-server/ ==="
          ls -la dist-server/ || echo "dist-server/ not found"

  windows-build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24.15.0"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build workspace packages
        run: npm run build:packages

      - name: Build client bundles
        run: npm run build:client

      - name: Build server bundle
        run: node scripts/build-server.mjs win32 x64

      - name: Build Windows sandbox helper
        run: node scripts/build-windows-sandbox-helper.mjs x64

      - name: Package Windows (unsigned NSIS installer)
        run: npx electron-builder --win nsis --publish never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"

      - name: Upload artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: 颉AI-Windows-x64
          retention-days: 30
          path: |
            dist/*.exe
          if-no-files-found: error
